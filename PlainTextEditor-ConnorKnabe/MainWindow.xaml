<Window x:Class="PlainTextEditor_ConnorKnabe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Content="Type your text in the box below" x:Name="txtLabel" Grid.Row="0" Grid.Column="1" HorizontalContentAlignment="Center"/>
        <ScrollViewer Grid.Row="1" Grid.ColumnSpan="2" Margin="10,10,9.667,89.333" Grid.RowSpan="2">
            <TextBox x:Name="txtInput"  TextWrapping="Wrap" AcceptsReturn="True" />


        </ScrollViewer>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                    VerticalAlignment="Bottom" Grid.Row="2" Grid.Column="1">
            <Button x:Name="btnSave" Content="Save" Width="75" Height="24" Margin="3" Click="btnSave_Click"/>
            <Button x:Name="btnOpen" Content="Open" Width="75" Height="24" Margin="3" Click="btnOpen_Click" />
        </StackPanel>

    </Grid>
</Window>
